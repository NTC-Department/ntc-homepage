interface Props {
  children: React.ReactNode;
}
const NeogangLayout = (props: Props) => {
  const { children } = props;

  // TODO: add transition animation
  return children;
};

export default NeogangLayout;
